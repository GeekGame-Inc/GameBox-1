<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="15dp"
    android:background="@drawable/radiu_white"
    android:padding="10dp" >

    <ImageView
        android:id="@+id/id_announcement_close"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:layout_alignParentTop="true"
        android:contentDescription="@string/empty"
        android:src="@drawable/icon_edit_delete" />

    <Button
        android:id="@+id/id_announcement_confirm"
        android:layout_width="match_parent"
        android:layout_height="35dp"
        android:layout_alignParentBottom="true"
        android:layout_marginLeft="15dp"
        android:layout_marginRight="15dp"
        android:layout_marginTop="2dp"
        android:background="@drawable/shape_rebate"
        android:gravity="center"
        android:text="@string/confirm"
        android:textColor="@color/white"
        android:textSize="14sp" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@id/id_announcement_confirm"
        android:layout_below="@id/id_announcement_close"
        android:orientation="vertical" >

        <TextView
            android:id="@+id/id_announcement_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:enabled="true"
            android:gravity="center"
            android:maxLines="1"
            android:padding="10dp"
            android:textColor="@color/textColor1"
            android:textSize="16sp" />

        <ScrollView
            android:layout_width="match_parent"
            android:layout_height="match_parent" >

            <TextView
                android:id="@+id/id_announcement_content"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:gravity="left"
                android:lineSpacingExtra="8dp"
                android:padding="10dp"
                android:textColor="@color/textColor1"
                android:textSize="14sp" />
        </ScrollView>
    </LinearLayout>

</RelativeLayout>