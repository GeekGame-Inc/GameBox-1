<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white"
    android:orientation="vertical" >

    <View
        android:layout_width="match_parent"
        android:layout_height="6dp"
        android:background="@color/divider" />

    <com.tenone.gamebox.view.custom.RefreshLayout
        android:id="@+id/id_classify_refreshLayout"
        android:layout_width="match_parent"
        android:layout_height="match_parent" >

        <com.tenone.gamebox.view.custom.MyExpandableListView
            android:id="@+id/id_classify_expandableView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:cacheColorHint="@color/transparent"
            android:childDivider="@color/divider"
            android:childIndicator="@null"
            android:divider="@color/divider"
            android:dividerHeight="1dp"
            android:groupIndicator="@null"
            android:listSelector="@color/transparent"
            android:scrollbars="none" >
        </com.tenone.gamebox.view.custom.MyExpandableListView>
    </com.tenone.gamebox.view.custom.RefreshLayout>

</LinearLayout>